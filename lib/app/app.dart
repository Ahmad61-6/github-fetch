import 'package:flutter/material.dart';

class GithubFetch extends StatelessWidget {
  const GithubFetch({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp();
  }
}
